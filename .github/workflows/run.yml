name: hello-world
on: 
  push:
  pull_request:
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: org
        run: echo "Hello O RG ORG [${{ secrets.ORG }}] ORG OR G"
      - name: repo
        run: echo "Hello R EPO REPO [${{ secrets.REPO }}] REPO REP O"
      - name: user
        run: echo "Hello U SR USR [${{ secrets.USR }}] USR US R"
      - name: shared
        run: echo "Hello O SH OSH [${{ secrets.SHR }}] OSH OS H"
      - name: shared
        run: echo "Hello U SH USH [${{ secrets.SHR }}] USH US H"


